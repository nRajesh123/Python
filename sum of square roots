/*Given two values A and B find out the following sum It is guaranteed that AB.
In other words, Given A and B, find out the SUM OF SQUARE ROOT OF ALL NUMBERS FROM A TO B (INCLUSIVE)
Input Format:
A single line input containing two space-separated integers. */


#include<stdio.h>
#include<math.h>
int main(){
    int a,b;
    float s=0.00;
    scanf("%d%d",&a,&b);
    while(a<=b){
        s+=sqrt(a);
        a++;
    }printf("%.2f",s);
}
